@extends('admin.layouts.admin')
@section('title',trans('marketplace::admin.moderation.title'))
@section('content')@include('marketplace::admin._nav')<h1>@lang('marketplace::admin.moderation.title')</h1>@forelse($resources as $resource)<div class="card mb-3"><div class="card-body"><h2 class="h5"><a href="{{ route('marketplace.resources.show',$resource) }}">{{ $resource->name }}</a></h2><p>{{ $resource->summary }}</p><small>{{ $resource->author->name }} · {{ $resource->category->name }}</small><div class="d-flex gap-2 mt-3"><form method="POST" action="{{ route('marketplace.admin.moderation.approve',$resource) }}">@csrf @method('PATCH')<button class="btn btn-success">@lang('marketplace::admin.moderation.approve')</button></form><form class="d-flex gap-2 flex-grow-1" method="POST" action="{{ route('marketplace.admin.moderation.reject',$resource) }}">@csrf @method('PATCH')<input class="form-control" name="moderation_note" required placeholder="@lang('marketplace::admin.moderation.reason')"><button class="btn btn-danger">@lang('marketplace::admin.moderation.reject')</button></form></div></div></div>@empty<div class="alert alert-info">@lang('marketplace::admin.moderation.empty')</div>@endforelse{{ $resources->links() }}@endsection
